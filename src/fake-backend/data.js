/**
 * @type DataBE
 */
const DATA = {

    employees: [...require('./data-employees')],
    teams: [...require('./data-teams')],
    requests: [...require('./data-requests')]

}

module.exports = DATA;